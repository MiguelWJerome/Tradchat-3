okay we need to revamp the gif system, rn if you notice it uses an api from giphy to search for gifs then checks them against the whitelist... we don't want that, remove the whitelist and make a new format for it following this schema: """Implement a locally-searched, whitelist-only GIF system to save on Giphy API quotas and ensure 100% content safety. First, use the Implementation Plan Tool to design two normalized tables in rooms.db: 1) whitelist_gifs with an INTEGER PRIMARY KEY id and a TEXT giphy_id, and 2) gif_tags with an INTEGER gif_id (FOREIGN KEY linking to whitelist_gifs.id with ON DELETE CASCADE) and a TEXT keyword. Ensure the keyword column is INDEXED for lightning-fast lookups. The server-side logic must handle a search by fetching all giphy_id values from whitelist_gifs that have a matching entry in gif_tags for the user's search term using a single SQL JOIN query. On the client side, do not store full URLs; instead, have the frontend reconstruct the Giphy URL on the fly using the giphy_id returned by the server. Include an admin-only route that allows us to manually insert a new Giphy ID and a list of approved keywords into these tables. This approach must bypass external API calls during user searches, relying entirely on our local database for "Trad-safe" results. Wait for plan approval before writing any code.""" we also need to remove any api related code from Server.py make it so when a gif is added to the system a nicely formatted gui html thingi comes up and askes them as soon as they click on the gif what keywords they want to add, (use html input boxes that adds more as they fill them in) they have to fill in at least 3 but they can do as many as they want. then send the gif id and keywords to the gif_whitelist.db in the schema format i shared above MAKE SURE TO LOWECASE ALL KEYWORDS WHEN THEY COME IN AND TO REMOVE ANY PUNCATION OR GO SPACES. when someone is retrieving from the homepage whenever they click enter check if any keywors START with their string their strings and remove go spaces and then that should be good for now...